// PLACEHOLDER — replace with the real Sentry IIFE bundle.
//
// Run this command from the extension root to fetch the real file:
//
//   curl -L -o src/vendor/sentry.bundle.min.js https://browser.sentry-cdn.com/8.45.0/bundle.min.js
//
// Until the real bundle is in place, this stub leaves `Sentry` undefined,
// which causes src/sentry/sentry-init.js to no-op gracefully. The extension
// keeps working with zero error reporting.
